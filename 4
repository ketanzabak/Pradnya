#include <stdio.h>
#include <string.h>
int main()
{
	char p[20];
	char *s = "Pradnya";
	int length = strlen(s);
	int i;
	for(i=0; i<length; i++)
		p[i] = s[length-i];
	printf("%s\n",p);

	return 0;
}

$a)ayndarP $b)ayndar $c)No Output $d)Error $3
